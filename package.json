{
  "name": "clickup-task-connect",
  "version": "1.0.0",
  "description": "A basic application to handle ClickUp Tasks via API",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "mongodb": "^6.21.0"
  },
  "devDependencies": {
    "vercel": "^30.2.2"
  }
}
